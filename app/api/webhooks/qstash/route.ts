// QStash webhook — Phase 2
import { NextResponse } from 'next/server';

export async function POST() {
  return NextResponse.json({ message: 'QStash webhook — Phase 2, not yet implemented' }, { status: 501 });
}
