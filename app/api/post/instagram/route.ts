// Instagram posting API — Phase 2
import { NextResponse } from 'next/server';

export async function POST() {
  return NextResponse.json({ message: 'Instagram posting API — Phase 2, not yet implemented' }, { status: 501 });
}
